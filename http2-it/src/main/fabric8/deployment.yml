spec:
  template:
    spec:
      containers:
        - name: vertx
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
            - containerPort: 8081
              name: aloha-http
              protocol: TCP
